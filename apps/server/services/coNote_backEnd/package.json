{
  "name": "conote-backend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "corepack pnpm exec tsx watch src/index.ts",
    "start": "node dist/index.js",
    "build": "rm -rf dist && corepack pnpm exec tsc -p tsconfig.json"
  },
  "dependencies": {
    "koa": "^2.16.1",
    "@koa/cors": "^5.0.0"
  }
}
