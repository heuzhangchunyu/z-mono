env: development
app_name: z-meng-backend

server:
  host: 127.0.0.1
  port: 4101

database:
  host: 127.0.0.1
  port: 5432
  user: postgres
  password: postgres
  db_name: z_meng
  ssl_mode: disable

runtime:
  enable_http: true
  migration_target: autoup
